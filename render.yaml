services:
  - type: web
    name: clipsave-downloader
    env: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: 10000
      - key: YTDLP_COOKIES
        value: "" # Optional: paste Netscape format cookies here for downloading age-restricted or premium videos
