# yt-dlp Desktop GUI

Minimal desktop app for downloading videos with `yt-dlp`.

## Run

```bash
python3 yt_dlp_gui.py
```

The app uses the `yt-dlp` command if it is installed. If that command is not available, it falls back to `python -m yt_dlp` when the Python package is installed.
# clipsave
